export default function Button({ children, variant = "primary", className = "", ...props }) { const styles = { primary: "bg-saffron text-white hover:opacity-95", secondary: "bg-deep-blue text-white hover:opacity-95", soft: "bg-cream text-deep-blue ring-1 ring-amber-200 hover:bg-amber-50" }; return <button {...props} className={`rounded-full px-5 py-3 text-sm font-medium transition ${styles[variant]} ${className}`}>{children}</button>; }
