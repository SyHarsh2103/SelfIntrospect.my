export default function SimpleCrudTable({ columns, rows }) { return <div className="overflow-x-auto rounded-3xl bg-white shadow-sm ring-1 ring-amber-100"><table className="min-w-full text-left text-sm"><thead className="bg-cream text-deep-blue"><tr>{columns.map((column) => <th key={column.key} className="px-4 py-3 font-medium">{column.label}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={row._id || index} className="border-t border-amber-100">{columns.map((column) => <td key={column.key} className="px-4 py-3 align-top text-slate-700">{typeof column.render === "function" ? column.render(row) : row[column.key]}</td>)}</tr>)}</tbody></table></div>; }
